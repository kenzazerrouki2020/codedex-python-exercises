trees = [["🌲","🌲","🌲"],
         ["🌲","🌲","🌲"],
         ["🌲","🌲","🌲"]]
rabit = "🐇"
for row in trees :
  print("".join(row))
position = input("enter the row and column where do you want the rabit become ")
row = int(position[0]) - 1
column = int(position[2]) -1
trees[row][column]= rabit

print("\nForest after placing the rabbit:\n")

for row in trees:
    print(" ".join(row))
